{
  "name": "pavilo",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal, self-hosted chat room that is ephemeral by default.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "config:check": "node config.js",
    "test": "node --test test/*.test.js",
    "test:browser": "node --test test/browser/*.test.cjs"
  },
  "dependencies": {
    "yaml": "2.9.0"
  }
}
