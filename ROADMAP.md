# Pavilo / 语亭路线图

这份路线图记录 Pavilo 已落地的基线与后续方向，不承诺固定发布日期。每个里程碑都应保持默认部署轻量、可理解、易维护。

## 长期原则

- 默认仍是无需数据库的临时聊天室，安装依赖后即可启动。
- 持久化、账号、风控等较重能力应可选启用，并与核心聊天逻辑解耦。
- 优先完善群聊、频道和部署体验，不追求成为全功能协作平台。
- 新协议和配置需要可测试、可迁移，并为后续版本保留兼容路径。
- 在认证、传输安全和滥用防护达到基线前，不宣称可以直接暴露到公网。

## 已实现：v0.1 Alpha 基线

- 临时内存聊天：文字、图片、回复、回应、输入状态和成员在线状态。
- 消息 ACK、客户端消息 ID 幂等去重、分块历史同步与房间纪元。
- 刷新及短暂断线可凭恢复令牌延续身份；全局和频道人数包含断线租约。
- 多频道服务端数据模型、频道切换，以及消息、成员、输入状态和历史隔离。
- 全局成员/连接、单 IP 连接、单频道人数、消息条数、频道字节、帧和写缓冲上限。
- WebSocket Origin、加入时限、心跳、发送频率、图片格式/尺寸/像素校验。
- 版本化 YAML 配置，支持频道、监听、容量、超时、频率和 IP 展示设置；包含严格校验与配置检查命令。
- HTTP 静态白名单，仅提供聊天页、样式表、房间接口及明确的自托管 vendor 资源。
- 静态文本资源按 `Accept-Encoding` 协商 gzip，压缩结果按 ETag 缓存。
- 图片发送前在浏览器内降采样到长边 1600 px 并收敛质量；GIF 保留动画。
- Node 原生配置、HTTP 和 WebSocket 自动化测试。

当前边界：默认显示频道成员 IP，以保持既有产品行为；可通过配置关闭。当前没有认证、只读频道角色、持久化或面向公网的完整防护。

**容量与尺寸的相互关系**：默认单图上限 300 KB 是按"一个频道能留住多少张图"反推的——1.5 MB 时一张原图就吃掉 32 MB 频道的约 5%，几张之后房间就开始淘汰历史。调大 `limits.maxImageBytes` 必须连同 `maxJsonBytes`、`maxWebSocketFrameBytes`、`maxWritableBytes`、`maxChannelBytes` 一起复核，配置校验会拒绝自相矛盾的组合但不保证组合是合理的。

## 下一步：配置与多频道稳定化

- 在真实局域网和反向代理部署中验证频道切换、容量上限与恢复行为。
- 继续完善多频道客户端状态、可访问性和移动端体验。
- 明确外部配置后续版本的兼容、废弃和迁移策略。
- 客户端协议、连接、pending、图片规则和状态归约已有独立 Node 测试；消息渲染与阅读位置单独测试，`index.html` 只保留页面结构与资源入口。
- 服务端领域状态与 HTTP/WebSocket transport 已分离，支持不启动网络的 core 测试。
- 独立 Chrome 双页面验收覆盖恢复、频道、失败重试、图片、阅读位置和移动端交互；继续推进 CI 与真机验证。
- 增加更大成员列表、图片和慢连接场景的性能与压力测试。
- 评估是否让客户端在不重载历史的前提下做增量渲染；当前淘汰会重建整个列表，阅读位置靠滚动锚点保持。

完成标准：多个频道可长期同时活跃，配置升级路径清晰，边界负载下资源使用可预测。

## 后续：部署与可选持久化

- 增加可选 SQLite 存储，覆盖频道、消息和必要的管理数据。
- 提供数据保留时长、容量配额、清理任务、备份和恢复机制。
- 增加容器镜像与示例编排文件，并完善私有服务器部署文档（含反向代理的最小可用配置）。
- 提供结构化日志与更完整的健康状态；现有 `/healthz` 只报告人数、连接数和消息字节数。
- 明确 `healthz`、`room-info` 等只读端点在规模更大或半公开部署下是否需要访问控制。
- 定义存储适配层，使内存与 SQLite 使用同一组核心接口。

完成标准：新用户仍可直接临时运行；需要长期运行的部署者可以显式启用 SQLite，并完成可验证的备份恢复。

## 后续：权限与风控

- 支持房间访问口令、邀请机制及可选账号体系。
- 增加管理员、频道管理者和普通成员角色，并在此基础上提供真正的只读频道。
- 支持禁言、踢出、封禁、举报、审计记录及申诉所需的基础信息。
- 强化文件类型识别、内容大小、存储配额、发送频率和异常行为限制。
- 为加入、发言、上传等关键事件提供可组合的风控扩展点。
- 补充反向代理信任边界、TLS/WSS、密钥管理和安全部署能力。

完成标准：管理操作可追踪，常见滥用行为可限制，安全配置具有明确默认值与验证方法。

## v1.0：稳定版

- 稳定外部配置、聊天协议和存储格式。
- 提供版本升级、数据迁移和回滚说明。
- 完成核心功能、安全边界、性能及兼容性测试。
- 完成部署、运维、备份、恢复、贡献和安全报告文档。
- 持续检查无障碍、移动端体验和多语言扩展能力。

## 候选功能池

以下能力根据真实需求评估，不绑定具体里程碑：

- 私聊与消息搜索
- PWA 安装与有限的离线体验
- 文件存储适配器和对象存储支持
- Webhook、机器人与管理 API
- 主题和界面语言扩展

## 默认不进入核心范围

- 语音或视频会议
- 多服务器联邦与分布式部署
- 强制依赖第三方云服务、外部账号或遥测平台
- 为少数高级功能显著增加默认安装和维护成本
