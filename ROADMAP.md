# Pavilo / 语亭路线图

这份路线图描述 Pavilo 从单频道 Alpha 原型走向稳定多频道聊天室的方向，不承诺固定发布日期。每个里程碑都应保持默认部署轻量、可理解、易维护。

## 长期原则

- 默认仍然是无需数据库的临时聊天室，一条命令即可启动。
- 持久化、账号、风控等较重能力应可选启用，并与核心聊天逻辑解耦。
- 优先完善群聊、频道和部署体验，不追求成为全功能协作平台。
- 新协议和配置需要可测试、可迁移，并为后续版本保留兼容路径。
- 在认证、传输安全和滥用防护达到基线前，不宣称可以直接暴露到公网。

## v0.1.x：基线加固

目标：在增加功能前，先让当前单频道原型具备清晰边界和可靠测试基础。

- 默认不向其他参与者暴露 IP，必要时由部署者显式开启。
- 增加全局消息内存、连接数、单 IP 连接和上传总量限制。
- 为 WebSocket 握手、分帧、心跳、断线和异常负载补充自动化测试。
- 增加可配置的 Origin 校验、监听地址、容量限制和超时参数。
- 整理客户端与服务端模块边界，保持默认安装零运行时依赖。

完成标准：核心聊天行为具备自动化覆盖，资源使用存在明确上限，默认配置不再向成员公开 IP。

## v0.2：多频道核心

目标：让多频道成为真实的数据模型和协议能力，而不是界面占位。

- 支持创建、列出、加入、离开和切换频道。
- 按频道隔离消息历史、在线成员、输入状态与系统通知。
- 保留 `general` 默认频道，让首次启动不需要额外配置。
- 为频道名称、人数、生命周期和临时历史设置清晰限制。
- 对 WebSocket 消息协议进行版本化，并为单频道客户端提供兼容路径。

完成标准：多个频道可以同时活跃，任何消息和成员状态都不会泄漏到其他频道。

## v0.3：部署与可选持久化

目标：方便长期运行和升级，同时不改变默认临时模式。

- 增加可选 SQLite 存储，覆盖频道、消息和必要的管理数据。
- 提供数据保留时长、容量配额、清理任务、备份和恢复机制。
- 增加容器镜像与示例编排文件，并完善私有服务器部署文档。
- 提供结构化日志、就绪检查和更完整的健康状态。
- 定义存储适配层，使内存与 SQLite 使用同一组核心接口。

完成标准：新用户仍可直接临时运行；需要长期运行的部署者可以显式启用 SQLite，并完成可验证的备份恢复。

## v0.4：权限与风控

目标：形成可以逐步面向更复杂网络环境的访问与治理基础。

- 支持房间访问口令、邀请机制及可选账号体系。
- 增加管理员、频道管理者和普通成员角色。
- 支持禁言、踢出、封禁、举报、审计记录及申诉所需的基础信息。
- 强化文件类型识别、内容大小、存储配额、发送频率和异常行为限制。
- 为加入、发言、上传等关键事件提供可组合的风控扩展点。
- 补充反向代理信任边界、TLS/WSS、密钥管理和安全部署指南。

完成标准：管理操作可追踪，常见滥用行为可限制，安全配置具有明确的默认值与验证方法。

## v1.0：稳定版

目标：提供稳定、可升级、文档完整的自托管多频道聊天室。

- 稳定外部配置、聊天协议和存储格式。
- 提供版本升级、数据迁移和回滚说明。
- 完成核心功能、安全边界、性能及兼容性测试。
- 完成部署、运维、备份、恢复、贡献和安全报告文档。
- 持续检查无障碍、移动端体验和多语言扩展能力。

## 候选功能池

以下能力根据真实需求评估，不绑定具体里程碑：

- 私聊、表情回应、消息搜索与浏览器通知
- PWA 安装与有限的离线体验
- 文件存储适配器和对象存储支持
- Webhook、机器人与管理 API
- 主题和界面语言扩展

## 默认不进入核心范围

- 语音或视频会议
- 多服务器联邦与分布式部署
- 强制依赖第三方云服务、外部账号或遥测平台
- 为少数高级功能显著增加默认安装和维护成本
